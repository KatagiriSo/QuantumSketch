/**
 * Enumerates the available drawing tools. Keeping them in one place makes it
 * easy to extend the editor with additional modes in the future.
 */
export type DrawMode = "normal" | "point" | "line" | "loop" | "string";
